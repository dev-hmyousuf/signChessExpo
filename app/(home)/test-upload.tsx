import { SafeAreaView } from "react-native";
import ServerUploadTest from "../components/ServerUploadTest";

export default function TestUploadPage() {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <ServerUploadTest />
    </SafeAreaView>
  );
}